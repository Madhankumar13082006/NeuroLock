apiVersion: v1
kind: ConfigMap
metadata:
  name: neurolock-config
data:
  NODE_ENV: "production"
  PORT: "3000"
  APP_URL: "http://localhost:3000"
  APPROVAL_BASE_URL: "http://localhost:3000"
  UNLOCK_DURATION_MS: "1800000"
  FALLBACK_DELAY_MS: "1200000"

